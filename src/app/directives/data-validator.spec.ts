import { DataValidator } from './data-validator';

describe('DataValidator', () => {
  it('should create an instance', () => {
    const directive = new DataValidator();
    expect(directive).toBeTruthy();
  });
});
